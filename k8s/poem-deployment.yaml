apiVersion: apps/v1
kind: Deployment
metadata:
  name: poem-service
  namespace: NAMESPACE
  labels:
    app: poem-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: poem-service
  template:
    metadata:
      labels:
        app: poem-service
    spec:
      containers:
      - name: poem-service
        image: REGION-docker.pkg.dev/PROJECT_ID/microservice-registery/poem-service:IMAGE_TAG
        ports:
        - containerPort: 8000
        env:
        - name: API_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: poem-secrets
              key: API_SECRET_KEY
        - name: ADMIN_TOKEN
          valueFrom:
            secretKeyRef:
              name: poem-secrets
              key: ADMIN_TOKEN
        - name: SERVICE_NAME
          valueFrom:
            configMapKeyRef:
              name: poem-config
              key: SERVICE_NAME
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: poem-service
  namespace: NAMESPACE
spec:
  selector:
    app: poem-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP

